# EchoMind 后端依赖（Python 3.12）
# 国内安装加速: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple

# Web 框架
fastapi>=0.115,<1
uvicorn[standard]>=0.30
python-multipart>=0.0.9

# 数据库
sqlalchemy>=2.0.30
psycopg[binary]>=3.2
pgvector>=0.3.4

# 配置
pydantic>=2.8
pydantic-settings>=2.4

# LLM（OpenAI 兼容协议客户端）
openai>=1.40

# 文档解析
pymupdf>=1.24          # PDF
python-docx>=1.1       # Word

# 检索
jieba>=0.42            # 中文分词
rank-bm25>=0.2.2       # BM25
numpy>=1.26,<3

# Embedding / Rerank（本地运行，首次启动会自动下载模型）
sentence-transformers>=3.0

# 缓存（阶段5使用，提前引入）
redis>=5.0

# 测试 / 工具
pytest>=8.0
httpx>=0.27
PyYAML>=6.0            # eval/run_eval.py 解析 questions.yaml
